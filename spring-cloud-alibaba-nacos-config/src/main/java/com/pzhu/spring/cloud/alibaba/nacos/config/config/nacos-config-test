spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

server:
  port: 8085

management:
  endpoints:
    web:
      exposure:
        include: "*"

username: guo-test
